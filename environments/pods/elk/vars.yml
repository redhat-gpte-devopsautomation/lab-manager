---

# Pod volumes
volumes:
  - /opt/podman/volumes/elk-volume/usr/share/elasticsearch/data

# Pod dockerfile if required along with pod-compose file
dockerfile: False
# Pod state
pod_service:
  state: started
  # Pod state upon reboot
  enabled: True



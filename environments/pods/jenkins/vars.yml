---

# Pod volumes
volumes:
  - /etc/jenkins/ssl/certs
# Pod dockerfile if required along with pod-compose file
dockerfile: True
config_templates:
  - src: jenkins.cfg.j2
    dest: /etc/jenkins/jenkins.cfg
# Pod state
pod_service:
  state: started
  # Pod state upon reboot
  enabled: True
---

# Pod volumes
volumes:
  - /etc/gitlab
  - /var/log/gitlab
  - /var/opt/gitlab
# Pod dockerfile if required along with pod-compose file
dockerfile: False
# Pod state
pod_service:
  state: started
  # Pod state upon reboot
  enabled: True
